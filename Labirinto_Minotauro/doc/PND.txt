pesquisa do trabalho
a ideas do pj
documentacao 
e apresentar para professora hj

resolver os conflitos
Com isso, a função resolver_labirinto((0, 0), (4, 4)) retornaria uma lista com o caminho mais curto em coordenadas!

👉 Agora posso montar o index.html com a visualização clicável do labirinto. Bora?

/* Fundo com imagem e overlay escuro */
body {
  background-image: url('image/Fundo.jpeg');
  background-size: cover;
  background-repeat: no-repeat;
  background-position: center;
  height: 100vh;
  margin: 0;
  font-family: greek freak;
  position: relative;
  z-index: 0;
}

body::before {
  content: "";
  position: fixed;
  top: 0; left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0,0,0,0.5); /* escurece o fundo */
  z-index: -1;
}

h1 {
  margin-top: 20px;
  color: #ffffff;
  text-align: center;
}

/* Labirinto */
.grid {
  display: grid;
  grid-template-columns: repeat(13, 30px);
  grid-template-rows: repeat(13, 30px);
  margin: 20px auto;
  width: fit-content;
}

.cell {
  width: 30px;
  height: 30px;
  border: 1px solid #ccc;
  background-color: white;
  box-sizing: border-box;
  transition: background-color 0.2s;
}

.cell.parede {
  background-color: black;
}

.cell.inicio {
  background-color: green;
}

.cell.fim {
  background-color: red;
}

.cell.caminho {
  background-color: yellow;
}

/* Botões */
button {
  margin: 10px 5px;
  padding: 8px 12px;
  font-size: 14px;
  border: none;
  background-color: #007bff;
  color: white;
  cursor: pointer;
  border-radius: 5px;
}

button:hover {
  background-color: #0056b3;
}